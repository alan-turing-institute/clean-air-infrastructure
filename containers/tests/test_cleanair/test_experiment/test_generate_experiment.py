"""Test air quality experiments are generated"""

# TODO lots of small issues to fix before we can do proper testing
# from cleanair.experiment.generate_air_quality_experiment import svgp_vary_static_features, VARY_STATIC_FEATURES

# def test_svgp_vary_static_features(secretfile, connection, fake_laqn_static_dataset):
#     """Test parameters are correct for varying static features"""
#     instance_list = svgp_vary_static_features(secretfile, connection=connection)
#     assert len(instance_list) == len(VARY_STATIC_FEATURES)
